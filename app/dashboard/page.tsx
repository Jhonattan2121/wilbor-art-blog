import HivePostButton from '@/components/HivePostButton';

export const dynamic = 'force-dynamic';

export default async function GridPage(props: any) {
 

  return (
    <div className="flex flex-row">
      <HivePostButton />
    </div>
  );
}